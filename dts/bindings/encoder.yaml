description: BM_ENCODER parent node

compatible: "encoder_counter"

include: base.yaml

properties:
    gpios:
      type: phandle-array
      required: true
      description: GPIO pin

    timer_id:
      type: int
      required: true
      description: TIMER instance id

    timer_address:
      type: int
      required: true
      description: TIMER instance address

    label:
      type: string
      description: Descriptive name of the key
    