description: ENCODER counter parent node

compatible: "encoder_counter"

include: base.yaml

properties:
    gpios:
      type: phandle-array
      required: true
      description: GPIO pin

    label:
      type: string
      description: Descriptive name of the key
    